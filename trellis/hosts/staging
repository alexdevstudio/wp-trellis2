# Add each host to the [staging] group and to a "type" group such as [web] or [db].
# List each machine only once per [group], even if it will host multiple sites.

[staging]
replace_with_domain_or_ip

[web]
replace_with_domain_or_ip
